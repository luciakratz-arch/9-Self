# Dependências existentes (não remova)
firebase-functions>=0.1.0
firebase-admin>=6.0.0
reportlab>=4.0.0

# Novas dependências para o webhook (já vêm no Python padrão, listadas para clareza)
# smtplib — built-in Python
# urllib.request — built-in Python
# json — built-in Python
